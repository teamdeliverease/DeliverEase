{
  "rules": {
    ".read": "auth.token.admin === true",
    "volunteers": {
      "$uid": {
        ".validate": "newData.hasChildren(['address', 'lat', 'lng', 'name', 'phone', 'email', 'timestamp'])",
        ".write": true
      }
    },
    "requesters": {
      "$uid": {
        ".validate": "newData.hasChildren(['address', 'lat', 'lng', 'name', 'phone', 'timestamp', 'list'])",
        ".write": true
      }
    }
  }
}
